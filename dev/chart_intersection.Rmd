---
title: "R Notebook"
output: html_notebook
---

```{r}
library(magrittr)
library(dplyr)
pool <- pool::dbPool(RSQLite::SQLite(), dbname = "inst/app/www/crash_db.db")


crash_df <-DBI::dbReadTable(pool, "2017crash")
# person_df <-DBI::dbReadTable(pool, "2017person")
# crash_df %>% mutate(CRSHDATE = lubridate::mdy(CRSHDATE))
```
```{r test wisinj chart}
crash <- wisdotcrashdatabase::import_db_data(filepath = "C:/CSV/csv_from_sas/fst/", db_type = "crash", columns = c(), years = c("17"))

# By CRSHSVR ?
RLTNJNLC

INTTYPE
crash %>% count(RLTNJNLC) 28645 + 10088 = 38733

crash %>% count(INTTYPE) 139970 - 20099 - 81429 = 38442
```
